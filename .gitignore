.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.DS_Store

# Cached model + encoded data. Regenerated by `make prepare`; too large to
# version, and derived entirely from the public IEEE-CIS files.
artifacts/*.pkl
artifacts/*.parquet

# reports/ IS tracked: those are the measured results.
